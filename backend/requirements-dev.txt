-r requirements.txt
pytest==8.3.5
httpx==0.28.1
# Model training (scripts/train_model.py) + unpickling the artifact at runtime.
# The base app runs without scikit-learn when no model artifact exists.
scikit-learn>=1.3,<1.7
